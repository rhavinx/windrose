services:
  windrose:
    image: rhavinx/windrose:latest
    container_name: windrose
    stop_grace_period: 30s
    environment:
      TZ: "UTC"
      # PUID: "1000"
      # PGID: "1000"
      SKIP_UPDATE: "0"
      SERVER_NAME: "My Windrose Server"
      INVITE_CODE: "changeme"
      SERVER_PASSWORD: "changeme"
      IS_PASSWORD_PROTECTED: "true"
      MAX_PLAYERS: "4"
      GAME_PORT: "7777"
      # USE_DIRECT_CONNECTION: "true"
      # DIRECT_CONNECTION_ADDRESS: "your.public.ip"  # stored for future game use; port forward + USE_DIRECT_CONNECTION=true is what enables external connections
      # REGION: ""
      # P2P_PROXY_ADDRESS: ""  # defaults to 0.0.0.0 (all interfaces) — only set to override
      # REMOVE_SERVER_FILES: "0"
    volumes:
      - /path/to/server:/home/steam/windrose/server
      - /path/to/data:/home/steam/windrose/data
    ports:
      - "7777:7777/udp"
      - "7777:7777/tcp"
    restart: unless-stopped
